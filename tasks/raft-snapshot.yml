---
- name: rm previous raft snapshot
  file:
    path: "/home/centos/raft.snap"
    state: absent

- name: take a raft snapshot
  shell: "source ~/.bashrc && {{ vault_bin_path }}/vault operator raft snapshot save raft.snap"
  